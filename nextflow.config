profiles {
    docker {
        process {
            executor = 'local'
            container = 'melclic/retrosynthesis:rp-0.1.0'
            debug = true
        }

        docker {
            enabled = true
            // runOptions = '--platform=linux/amd64'
        }
    }
}
